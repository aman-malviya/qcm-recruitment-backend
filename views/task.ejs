<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <link rel="icon" href="/docs/4.0/assets/img/favicons/favicon.ico" />

    <title>Assign Tasks</title>

    <link
      rel="canonical"
      href="https://getbootstrap.com/docs/4.0/examples/sign-in/"
    />

    <!-- Bootstrap core CSS -->
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
      integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk"
      crossorigin="anonymous"
    />

    <!-- Custom styles for this template -->
    <link href="CSS/styles.css" rel="stylesheet" />
  </head>

  <body class="text-center">
    <form action="/tasks" class="form-signin" method="POST">
      <h1 class="h3 mb-3 font-weight-normal">Assign a task</h1>
      <select class="form-control" name="username" id="">
        <!-- Loop -->
         <% for(i=0;i<foundUsers.length;i++){ %>
        <option value="<%= foundUsers[i].name %>"><%= foundUsers[i].name %></option>
        <% } %>
      </select>
      <input
        name="task"
        class="form-control middle"
        type="text"
        placeholder="Task"
        required
      />
      <select class="form-control" name="taskStatus" id="">
          <option value="complete">Complete</option>
          <option value="incomplete">Incomplete</option>

      </select>
      <button class="btn btn-lg btn-primary btn-block" type="submit">
        Assign Task
      </button>
      <p class="mt-5 mb-3 text-muted">&copy; Aman Malviya</p>
      <a class="task-link" href="/">Add a User</a>
    </form>
  </body>
</html>
